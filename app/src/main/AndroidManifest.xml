<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zjrb.coreproject">
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:configChanges="orientation|keyboardHidden|screenSize"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.TestLoadindActivity"
            android:label="@string/title_activity_test_loadind" />
        <activity android:name=".activity.TestCacheActivity" />
        <activity android:name=".activity.TestPictureActivity" />
        <activity android:name=".demo.empty.EmptyPageSimpleActivity" />
        <activity android:name=".demo.overlay.OverlaySimpleActivity" />
        <activity android:name=".activity.TestCommentActivity" />
        <activity android:name=".activity.TestScoreNotifyActivity" />
        <activity android:name=".activity.TestGlideActivity" />
        <activity android:name=".activity.TestAudioRecordActivity" />
        <activity
            android:name=".activity.TestWebViewActivity"
            android:configChanges="orientation|keyboardHidden|screenSize" />
        <activity android:name=".activity.TestWhiteTopBarActivity" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity android:name=".activity.TestFragmentActivity"></activity>
    </application>

</manifest>